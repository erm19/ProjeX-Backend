{
  "name": "@urbanix/rabbitmq",
  "version": "1.0.3",
  "description": "Shared package for RabbitMQ connection and event handling logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [],
  "author": "Eyal Moskowitz",
  "license": "ISC",
  "devDependencies": {
    "@types/amqplib": "^0.10.6",
    "@types/express": "^5.0.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@urbanix/error-handling": "^1.3.1",
    "amqplib": "^0.10.5"
  }
}
